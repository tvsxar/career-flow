services:
  backend:
    build:
      context: ./backend

    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend

    ports:
      - "8080:80"

    depends_on:
      - backend
